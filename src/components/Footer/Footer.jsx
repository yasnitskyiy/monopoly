import './Footer.scss'
import '../../App.scss'

const Footer = (props) => {
    return (
        <div className={`footer`}>
            <div className={`center`}>

            </div>
        </div>
    )
}

export default Footer